<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      <nav class="navbar navbar-inverse">
        <div class="container-fluid">
          <div class="navbar-header">
           <a class="navbar-brand" href="#">Bookers</a>
          </div>
      <% if user_signed_in? %>
          <ul class="nav navbar-nav navbar-right">
            <li class="Home"><%= link_to "Home",user_path(current_user.id)%></li>
            <li class="users"><%= link_to "Users",users_path%></li>
            <li class="books"><%= link_to "Books",books_path%></li>
            <li class="logout"><%= link_to "logout", destroy_user_session_path, method: :delete %></li> -->
          </ul>
      <% else %>
          <ul class="nav navbar-nav navbar-right">
            <li class="Home"><%= link_to "Home",root_path%></li>
            <li class="About"><%= link_to "About",home_about_path%></li>
            <li class="signup"><%= link_to "signup",new_user_registration_path%></li>
            <li class="login"><%= link_to "login",new_user_session_path%></li>
          </ul>
      <% end %>
        </div>
      </nav>
<% flash.each do |key, value| %>
  <%= content_tag(:div, value, class: "flash flash__#{key}") %>
  <% end %>

    <%= yield %>
    <footer>
      <div class="container">
        <div class="row">
          <div class="col-xs-3 col-xs-offset-9">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>


